#pragma once

namespace ab {

/// MIME type carrying a catalog typeId when dragging an element from the
/// palette onto the canvas.
inline constexpr const char* kNodeMimeType = "application/x-avena-node";

} // namespace ab
